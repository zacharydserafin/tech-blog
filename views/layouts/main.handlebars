<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
</head>


<body>
    <header>
        <h1 class="center-flex title">Tech Blog</h1>
    </header>
    <nav class="center-flex m5">
        <a class="border m5" href="/">Homepage</a>
        <a class="border m5" href="/dashboard">Dashboard</a>
        {{#if logged_in}}
        <a class="border m5" href="/" id="logout">Log Out</a>
        {{else}}
        <a class="border m5" href="/login">Log In/Sign Up</a>
        {{/if}}
    </nav>
    <main>
      {{{body}}}
    </main>
    {{#if logged_in}}
    <script src="/js/logout.js"> </script>
    {{/if}} 
</body>
</html>